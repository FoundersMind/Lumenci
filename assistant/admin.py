"""
Django admin is intentionally minimal: cases, charts, rows, docs, chat, and
change history are managed from the Lumenci Assistant single-page UI (/).
"""

from django.contrib import admin

# Register models here only if you need emergency /admin access; the product
# UI exposes the same data operations via JSON APIs.
